# SVD Image Compression

Kompresja obrazów metodą SVD.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install numpy pillow matplotlib jupyter ipykernel
```

## Obrazy testowe

Znajdują się w images `images/` 